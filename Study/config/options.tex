\geometry{
  b5paper, % 'a4paper', 'b5paper', 'letterpaper', 'legalpaper'
  asymmetric, % don't swap margins. as opposed to twoside
  centering, % center the content between margins
  bindingoffset=0cm,
}
\graphicspath{ {./figures/} }
